<template>
    <div :class="['dropdown-menu', (align === 'right') ? 'dropdown-menu-right' : '']">
        <h6 v-if="header != null" class="dropdown-header">{{header}}</h6>
        <slot></slot>
    </div>
</template>

<script>
    export default {
        props: {
            header: {
                type: String
            },
            align: {
                type: String,
                default: ''
            }
        }
    }
</script>