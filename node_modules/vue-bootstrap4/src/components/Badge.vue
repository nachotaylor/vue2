<template>
    <span :class="['badge',`badge-${type}`, pill ? 'badge-pill' : '']"><slot></slot></span>
</template>

<script>
export default {
    props: {
        type: {
            type: String,
            default: 'default'
        },
        pill: {
            type: Boolean,
            default: false
        }
    }
}
</script>